
spring:
  application:
    name: postman
  cloud:
    config:
      uri: http://localhost:8080/api/config
  mail:
   host: smtp.gmail.com
   username: artempomanov@gmail.com
   password: "freyhjhxnopqnbfl"
   properties:
    mail:
      transport:
        protocol: smtp
      smtp:
        ssl:
          trust: "*"
        port: 587
        auth: true
        starttls:
          enable: true
          required: true
server:
  port: 10002