server:
   port: 10000
spring:
  application:
    name: storage
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8080/api/config
  datasource:
    url: jdbc:postgresql://localhost:5432/recipesStorage
    username: "Artem"
    password: "000"
  jpa:
    hibernate:
      ddl-auto: none
